# Dependencias do relatorio de vendas EVO.
#
# O acesso ao EVO, o envio de e-mail, os graficos do e-mail (SVG inline +
# tabelas HTML) e a chamada da API de WhatsApp usam apenas a biblioteca
# padrao do Python 3.
#
# O Pillow abaixo e OPCIONAL: serve so para desenhar o relatorio em PNG e
# anexar na notificacao do WhatsApp. Sem ele a mensagem sai apenas com o
# texto e o job continua normalmente.
#
# Nao ha nada a instalar alem do pip - nem navegador, nem lib de sistema.
# O wheel do Pillow ja vem com tudo compilado e a fonte usada no desenho
# esta versionada em assets/fonts.
pillow>=10.1
